#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include "menu.h"
#include "mascota.h"
#define TAM 30
#define LEN 5
#define VACIO 0
#define OCUPADO 1

/*
1-Se debe mostrar un listado de mascotas con sus razas y países de origen

#Borrado lógico de una mascota del listado
#-Se debe mostrar un listado de mascotas ordenadas por país de origen.

Es cargar una estructura en el array de esas estructuras.
Primero te fijas con alguna funcion si hay lugar disponible en el array
(una posicion con el campo isEmpty==1). Si hay lugar pedis los datos al usuario y los pasas a una estructura auxiliar (
ejemplo, eMascota mascotaAux). Despues que pedis todos los datos al usuario (salvo el id que lo debe asignar el programa),
copias ese auxiliar a la posicion liubre del array.

#-Alta de mascota, se pueden cargar todos los datos y se debe mostrar el listado de
razas disponibles, teniendo en cuenta que ya tengo 5 mascotas cargadas y que el
máximo es 10
*/



int main()
{

    MostrarMenu();

}





